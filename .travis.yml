language: php

php:
  - 7.1
  - 7.2
  - 7.3

dist: xenial
  
install:
  - composer install
  - composer validate
  
before_script:
  - pecl channel-update pecl.php.net
  - pecl install ast
  - phpenv config-rm xdebug.ini

script:
  - vendor/bin/phan
  - composer test